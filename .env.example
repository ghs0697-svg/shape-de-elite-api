# Vars que precisam ser configuradas no Vercel:

# Upstash KV (mesma instance do CRM se quiser, ou nova)
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# Webhook Greenn — gerar string aleatória forte
GREENN_WEBHOOK_SECRET=

# Admin endpoint pra grant/revoke manual — outra string aleatória
ADMIN_SECRET=

# Lista de emails pré-autorizados (sem precisar de webhook) — separados por vírgula
SHAPE_ALLOW_EMAILS=ghs0697@gmail.com
